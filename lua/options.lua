-- Static editor settings. These are simple key/value flags that control how Neovim behaves and looks. Things like line numbers, tab width, mouse support, clipboard, word wrap. They don't react to events like autocmds.
require("nvchad.options")
